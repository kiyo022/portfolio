// src/types/index.ts\n\n// Type definition for a Customer\nexport interface Customer {\n    id: number;\n    name: string;\n    email: string;\n    phone?: string; // optional\n    createdAt: string; // ISO date string\n}\n\n// Type definition for a Note\nexport interface Note {\n    id: number;\n    customerId: number; // relates note to customer\n    content: string;\n    createdAt: string; // ISO date string\n}\n\n// Type definition for API Responses\nexport interface ApiResponse<T> {\n    success: boolean;\n    message?: string;\n    data?: T; // generic type for response data\n}